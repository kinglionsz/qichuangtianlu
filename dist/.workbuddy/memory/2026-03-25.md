# 2026-03-25

## Vite 构建准备 - 骑闯天路项目重构

**项目路径**: `D:\ampa_migra\G\CodeSources\ai_project\mimo\bike-project`
**工作空间**: `D:\ampa_migra\G\CodeSources\ai_project\mimo\bike-project\dist`（原始单HTML文件）

### 完成内容

对 `dist/index.html`（3041行单体HTML）进行代码审核，重构为 Vite 项目：

**问题清单（已全部处理）**：
1. 单体 HTML：3041行，内联 CSS/JS/外部注入代码混杂
2. MiniMax 平台注入代码：`inland-security-footer` + 3段重复 iframe 高亮脚本（约1500行）→ 全部剔除
3. `bike.jpg` 缺失引用 → Canvas BG overlay 改用已有的 `webwxgetmsgimg_2.jpg`
4. 外部字体 `./骑...files/css2` 本地缓存路径 → 改回 Google Fonts CDN `@import`
5. Lightbox `<img src="https://kyzv5635w9sh.space.minimaxi.com/">` 硬编码平台URL → 改为空 src
6. 一级虐点仍有 `Math.sin(Date.now()/200)` 未优化 → 改用缓存 `pulseValue`
7. `<!-- saved from url -->` 注释 → 删除

**新项目结构**：
```
bike-project/
├── index.html              # 干净的 Vite 入口 HTML
├── vite.config.js          # Vite 配置（outDir: dist-build）
├── package.json            # vite@5.4 devDep
├── .gitignore
├── public/
│   └── imgs/               # 11张图片（静态资源，直接复制）
└── src/
    ├── styles/main.css     # 全量CSS（含Google Fonts @import）
    └── js/
        ├── main.js         # 入口：import CSS + 挂载 window API
        ├── ui.js           # Scroll Reveal / Lightbox / Menu / Gallery
        ├── trajectory.js   # Canvas 渲染引擎（30fps + 脉冲缓存）
        └── trajectoryData.js # 路书数据 (waypoints/checkpoints/coastPts)
```

**构建结果**（`npm run build` ✅ 零错误）：
- `dist-build/index.html` 18.08 kB (gzip 5.78 kB)
- `dist-build/assets/index-*.css` 22.86 kB (gzip 5.04 kB)
- `dist-build/assets/index-*.js` 18.06 kB (gzip 5.96 kB)
- `dist-build/imgs/` 11张图片全部输出

**注意**：原 `dist/` 目录保留完整备份，不受影响。

## 图片去重 - webwxgetmsgimg_2.jpg → route-map.jpg

`webwxgetmsgimg_2.jpg` 与 `route-map.jpg` 内容相同，统一使用 `route-map.jpg`：
- `src/styles/main.css` ✅ 已替换
- `src/js/trajectory.js` ✅ 已替换
- `dist/index.html` ✅ 已替换
- `dist/working-backup.html` ✅ 已替换
- 重复文件 `webwxgetmsgimg_2.jpg` 需用户手动删除（命令被环境拦截）
